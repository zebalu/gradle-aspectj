apply plugin: 'groovy'
apply plugin: 'maven-publish'

group = 'net.uberfoo.gradle'
version = '2.3-SNAPSHOT'

repositories {
    maven {
        url "s3://uberfoo-maven/releases"
		credentials(AwsCredentials) {
			accessKey AWS_ACCESS_KEY
			secretKey AWS_SECRET_KEY
		}
    }
    maven {
        url "s3://uberfoo-maven/snapshots"
		credentials(AwsCredentials) {
			accessKey AWS_ACCESS_KEY
			secretKey AWS_SECRET_KEY
		}
    }
}

dependencies {
    compile localGroovy()
    compile gradleApi()
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
    repositories {
        maven {
            url "s3://uberfoo-maven/${project.version.endsWith('-SNAPSHOT') ? 'snapshots' : 'releases' }"
            credentials(AwsCredentials) {
                accessKey AWS_ACCESS_KEY
                secretKey AWS_SECRET_KEY
            }
        }
    }
}

